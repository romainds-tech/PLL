{% extends 'base.html.twig' %}

{% block title %}LanguageExecution{% endblock %}

{% block body %}
    <h1>LanguageExecution</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ language_execution.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ language_execution.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_language_execution_index') }}">back to list</a>

    <a href="{{ path('app_language_execution_edit', {'id': language_execution.id}) }}">edit</a>

    {{ include('language_execution/_delete_form.html.twig') }}
{% endblock %}
