{% extends 'base.html.twig' %}

{% block title %}LanguageExempleType index{% endblock %}

{% block body %}
    <h1>LanguageExempleType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for language_exemple_type in language_exemple_types %}
            <tr>
                <td>{{ language_exemple_type.id }}</td>
                <td>{{ language_exemple_type.name }}</td>
                <td>{{ language_exemple_type.description }}</td>
                <td>
                    <a href="{{ path('app_language_exemple_type_show', {'id': language_exemple_type.id}) }}">show</a>
                    <a href="{{ path('app_language_exemple_type_edit', {'id': language_exemple_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_language_exemple_type_new') }}">Create new</a>
{% endblock %}
