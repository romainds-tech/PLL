<form method="post" action="{{ path('app_language_exemple_delete', {'id': language_exemple.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ language_exemple.id) }}">
    <button class="btn">Delete</button>
</form>
