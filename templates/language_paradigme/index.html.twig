{% extends 'base.html.twig' %}

{% block title %}LanguageParadigme index{% endblock %}

{% block body %}
    <h1>LanguageParadigme index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for language_paradigme in language_paradigmes %}
            <tr>
                <td>{{ language_paradigme.id }}</td>
                <td>
                    <a href="{{ path('app_language_paradigme_show', {'id': language_paradigme.id}) }}">show</a>
                    <a href="{{ path('app_language_paradigme_edit', {'id': language_paradigme.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_language_paradigme_new') }}">Create new</a>
{% endblock %}
