{% extends 'base.html.twig' %}

{% block title %}Paradigme{% endblock %}

{% block body %}
    <h1>Paradigme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paradigme.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ paradigme.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_paradigme_index') }}">back to list</a>

    <a href="{{ path('app_paradigme_edit', {'id': paradigme.id}) }}">edit</a>

    {{ include('paradigme/_delete_form.html.twig') }}
{% endblock %}
